# TODO

- [x] Inspect target UI elements affected by “layout border font spacing text and color” (Layout + Breadcrumbs + global.css + theme.js).
- [x] Implement visual fixes: unify border color/thickness + typography weight/letter-spacing + consistent spacing (py/px/gap) + use theme primary/pink colors instead of hardcoded where needed.
- [x] Update `client/src/features/blog/components/Layout.jsx` (AppBar borderBottom + footer borderTop + Toolbar spacing/alignment).
- [x] Update `client/src/components/Breadcrumbs.jsx` (breadcrumb container spacing + separator/icon + hover/active colors).
- [ ] Update `client/src/theme.js` (ensure consistent typography + component overrides for borders/padding/spacing if missing).
- [x] Update `client/src/assets/styles/global.css` (remove duplicated blocks / normalize border/selection/focus styling).
- [x] Run frontend build/lint/tests to verify no regressions.
- [ ] Provide command to view/apply changes.


