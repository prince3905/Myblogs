{
  "name": "blogging-web-mern",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "npm run build && cd server && node src/server.js",
    "dev": "concurrently \"cd server && node src/server.js\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
